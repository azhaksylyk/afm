# Автоматическая категоризация наименований ТРУ (без разметки)

Этот проект реализует автоматическую **кластеризацию и категоризацию наименований ТРУ** (товаров, работ, услуг) **без ручной разметки** и без необходимости создавать `train.csv`.

Используется метод машинного обучения **без учителя** (unsupervised learning), который:

- вычисляет эмбеддинги текста с помощью `SentenceTransformer` (BERT-модель);
- группирует наименования по смыслу с помощью `KMeans`;
- извлекает ключевые термины через `TF-IDF`;
- автоматически формирует **названия категорий**.

В результате создаётся новый CSV-файл, где каждому наименованию сопоставлены:

- `cluster_id` — номер смыслового кластера,
- `auto_category` — автоматически сгенерированная категория,
- `cluster_top_terms` — ключевые слова, которые определяют категорию,
- `description` — исходный текст.

---

## 📁 Структура проекта

```
AFM/
│  README.md
│  requirements.txt
│  .gitignore
└─ data/
│    some_file.csv       # входной файл (кладёшь сюда)
│    result_file.csv             # выходной автосгенерированный файл (создаётся автоматически)
└─ src/
     __init__.py
     preprocessing.py
     auto_cluster.py
```

---

## 🔧 Установка и запуск

### 1. Скачать и открыть папку проекта

```bash
git clone https://github.com/azhaksylyk/afm.git
```

### 2. Создать окружение

```bash
python -m venv .venv
source .venv/bin/activate       # macOS/Linux
# .venv\Scripts\activate      # Windows
```

### 3. Установить зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Положить входной файл в папку `data/`

Например:

```
data/some_file.csv
```

### 5. Запуск категоризации

```bash
python -m src.auto_cluster   --input-path data/some_file.csv   --output-path data/result_file.csv
```


---

## 🧩 Дальнейшее использование

- можно сразу использовать `auto_category` в отчётах;
- можно переименовать категории вручную;
- можно обучить supervised-классификатор поверх полученных кластеров;
- можно автоматизировать обработку новых файлов.

Готово. Этот проект является полностью автоматическим решением для первичной категоризации ТРУ.
